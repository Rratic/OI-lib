请相应部分代码书写者本人在OJ上（通常选用洛谷模板题）进行测试，模板如下：
```cpp
#include <bits/stdc++.h>
using namespace std;
// 开发者测试 | 欢迎访问 https://github.com/OIalgorithm/OIalgorithm
typedef long long ll;
typedef unsigned long long ull;
void SetupIO(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}
// 您要写在本项目中的代码
int main(){
    SetupIO();
    // 实例
    return 0;
}
```
